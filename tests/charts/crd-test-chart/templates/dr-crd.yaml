{{- if .Values.global.installCRDs }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: notebooks.datarobot.com
spec:
  group: datarobot.com
  names:
    kind: Notebook
    plural: notebooks
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          description: "example brace {{ "{{ .foo }}" }}"
          properties:
            spec:
              type: object
{{- end }}
